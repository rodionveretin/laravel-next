// import '../../public/assets/sass/pages/settings.scss';

export default function Posts() {
	// const response = await fetch(`http://backend:9000/api/settings`, {
	// 	cache: 'no-store',
	// });

	// const settings = await response.json();

	return (
		<>
			{/* <div className="settings">
				<div className="settings__container">
					<h1 className="title settings__title">Настройки</h1>
					<Settings initialSettings={settings} />
				</div>
			</div> */}
		</>
	);
}
