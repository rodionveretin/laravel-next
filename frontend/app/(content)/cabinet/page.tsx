'use client';

import { useEffect, useState } from 'react';
import Image from 'next/image';
// import styles from '../../public/assets/sass/app.module.scss';

export default function Page() {
	// const [data, setData] = useState({ name: '', descr: '' });
	// const [loading, setLoading] = useState(true);

	// useEffect(() => {
	// 	fetch('/api/test')
	// 		.then((response) => response.json())
	// 		.then((data) => {
	// 			setData(data);
	// 			setLoading(false);
	// 		})
	// 		.catch((error) => {
	// 			console.error('Error fetching data:', error);
	// 			setLoading(false);
	// 		});
	// }, []);

	// if (loading) {
	// 	return (
	// 		<div className="loading-screen">
	// 			<div className="loading loading--dark loading--medium">
	// 				<Image src="/assets/images/loading.svg" alt="Загрузка..." width={64} height={64} />
	// 				<div className="loading__text">Загрузка...</div>
	// 			</div>
	// 		</div>
	// 	);
	// }

	return <></>;
}
